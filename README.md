# Set up 
initialize git
    
    git init

make first commit

    git add .
    git commit -m ""

# Create github repo
Add repo as remote and push it to main

    git remote add origin <repo-url>
    git branch -M main
    git push -u origin main

    